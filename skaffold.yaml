apiVersion: skaffold/v2beta23
kind: Config
metadata:
  name: skaffold-testrepo
build:
  artifacts:
  - image: my-nginx
    docker:
      dockerfile: Dockerfile
deploy:
  kubectl:
    manifests:
    - manifests/deployment.yaml
